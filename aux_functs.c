/*Header ->*/
#include "main.h"

/*Funct to comprobate a correct _printf ->*/
int correct_printf(char *buffer, const char *format){

    if (!buffer || !format)
        return (0);

    else 
        return (1);
}
